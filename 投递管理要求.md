# 秋招投递管理要求

> 本文档是秋招投递管理的**总纲**。所有目录规范、简历复用流程、管理平台需求都记录在这里。  
> 后续有新增或调整要求，直接在这个文件里追加/修改，并 commit 到 git。

**核心约定**：平台开发完成后，你（cuz）不手动操作系统，由我（小铜罩）负责平台的全部数据维护——你下令投递，我主动记录；你有消息，我主动更新。

---

## 一、目标

- 在本地空间按类别系统化管理秋招投递（多家公司、多个岗位）。
- 每次投递都有独立的公司文件夹，便于存放针对性修改的简历与投递材料。
- 后续搭建一个**本地可视化管理平台**，统一查看/筛选/分析投递全貌，并记录秋招过程中遇到的问题。

---

## 二、目录结构规范

根目录（当前空间）下按投递类型分三类：

```
秋招投递/
├── 后端开发/        # Java 后端等后端岗位
├── 测试开发/        # 测试开发岗位
├── AI开发/          # AI 应用等岗位
├── 投递管理要求.md   # 本文件
└── .gitignore
```

**命名语言（个人喜好）**：所有文件夹与文件一律使用**中文命名**，不用英文 slug。公司名、岗位名、说明均用中文。

**分类归属规则**：投递时由我（小铜罩）根据你指定的类型，自动把公司文件夹建到对应分类目录下。  
三个类型：后端开发 / 测试开发 / AI开发。

**公司文件夹命名**：在每个分类目录下，按公司建一级文件夹。

- 建议命名：`公司名`（如 `字节跳动`、`蚂蚁集团`）。
- 若同一公司投多个岗位且需分开管理，用 `公司名_岗位` 区分（如 `字节跳动_后端开发`）。

**每次投递子文件夹**：每家公司每次投递新建一个子文件夹。

- 建议命名：`投递N_YYYYMMDD`（如 `投递1_20260904`、`投递2_20260910`）。
- 内含：针对性修改后的简历 PDF + 该次投递的说明/材料。

> 命名规范如需调整，在此处修改后即可，不必重来。

---

## 三、简历来源与复用流程

- **初始简历来源**：资料库「简历」目录下的 `柳铜罩简历.pdf`（v16d，2026-09-03 17:54 导出）。
- **复用流程**：
  1. 投某家公司时，从资料库「简历」目录复制初始简历到对应公司/投递文件夹。
  2. 针对该公司岗位做**必要的针对性修改**（项目侧重、技能排序、关键词匹配 JD 等）。
  3. 修改后的简历保存在该公司投递文件夹内，文件名带版本/公司标识（如 `柳铜罩_字节跳动_后端.pdf`）。
- **原则**：不编造任何未经你确认的数据；量化数字以你确认过的为准。

---

## 四、待开发：本地可视化管理平台需求

> 本次**暂不开发**，仅记录需求与技术方案。开发指令下达后以本节为准，可在此增删字段。

### 4.0 技术方案（已确认）

- **架构**：前后端分离。
- **前端**：Vue。
- **后端**：FastAPI（Python）。
- **数据库**：SQLite（轻量本地文件库，无需独立服务进程）。
- **文件/图片存储**：MinIO（对象存储，用于简历 PDF、笔试/面试截图、证明材料等二进制文件）。
- **部署形态**：本地运行（前端 dev server 或构建静态产物 + FastAPI 提供接口与服务），不依赖外部云服务。
- **数据维护责任（重要）**：平台开发完成后，**你不需要手动操作系统**。由我（小铜罩）负责平台数据管理——
  - 你下达「投递 某公司 / 岗位（类型）」指令 → 我主动建目录、复制并修改简历、在平台录入投递记录。
  - 你有进展消息（笔试 / 面试 / offer / 挂了 / 拒接等）→ 告诉我 → 我更新对应记录状态与备注。
  - 秋招中遇到的问题 → 我记录到问题模块，可关联投递记录。

### 4.1 投递记录（核心表）

| 字段       | 说明                                             | 是否可筛选   |
| -------- | ---------------------------------------------- | ------- |
| 公司名称     | 必填                                             | 是       |
| 公司别名/英文名 | 选填                                             | 是       |
| 投递类型     | 后端开发 / 测试开发 / AI开发                             | 是（核心维度） |
| 岗位名称     | 必填                                             | 是       |
| 投递状态     | 待投递 / 已投递 / 笔试中 / 面试中 / 已挂 / 已Offer / 已拒绝 / 爽约 | 是（核心维度） |
| 投递渠道     | 官网 / 内推 / 招聘平台(Boss·猎聘·校招等) / 邮箱               | 是       |
| 投递日期     | YYYY-MM-DD                                     | 是       |
| 工作城市     | 如 杭州 / 北京 / 上海 / 远程                            | 是       |
| JD 地址/链接 | 岗位链接或投递入口                                      | 否       |
| 简历版本     | 对应文件夹内的简历文件                                    | 否（可跳转）  |
| 招聘要求     | 学历/技能/经验要求                                     | 否       |
| 针对性修改说明  | 该次简历改了什么                                       | 否       |
| 内推人      | 谁内推                                            | 是       |
| 薪资范围     | 如 20-30K·月                                     | 否       |
| 截止日期     | YYYY-MM-DD                                     | 是       |
| 备注       | 自由文本                                           | 否       |

### 4.2 问题记录（秋招过程）

记录投递/笔试/面试中遇到的问题，方便回头复盘：

| 字段      | 说明                     |
| ------- | ---------------------- |
| 标题      | 一句话                    |
| 分类      | 面试 / 笔试 / 流程 / 技术 / 其他 |
| 关联公司/岗位 | 可关联投递记录                |
| 问题描述    | 具体情况                   |
| 解决方案/答案 | 怎么解决的                  |
| 标签      | 如 算法 / 八股 / HR面        |
| 记录日期    | YYYY-MM-DD             |

### 4.3 平台能力要求

- 列表/看板视图查看每一家公司：岗位、简历、要求、类型、地址、状态等。
- 支持按**类型、状态、城市、时间、公司、渠道**多维度筛选与排序。
- 支持统计：各类型投递数、各状态分布、时间趋势。
- 问题记录模块独立但可关联投递。
- 数据本地存储（优先文件/轻量数据库，不依赖外部服务）。
- 后续可扩展：导出报表、提醒 deadline。

---

## 五、待补充 / 后续新增要求

- [x] 平台技术栈：Vue + FastAPI + SQLite + MinIO（已确认，见 4.0）。
- [x] 公司/文件命名：一律中文命名（个人喜好，已确认）。
- [x] 数据维护方式：由我（小铜罩）主动维护，你不下场操作（已确认）。
- [ ] 是否需要自动从本地文件夹结构反向同步到平台（你投一次我用脚本扫目录入库），还是每次我手动录入？开发前确认。
- [ ] MinIO 是否需要启用永久链接 / 桶策略细节？开发时定。
- [ ] 其他你随时补充……
